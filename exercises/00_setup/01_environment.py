"""Setup · your first solve()

Every Python exercise is one file with one function called
`solve`. You declare what data you need by naming it as a
parameter — here, `trades` — and the lab hands you that table
as a pandas DataFrame.

Available names: clients, securities, prices, trades,
positions, plus `con` (a DuckDB connection) and `raw_dir`
(the vendor files).

TASK
    Return the number of rows in `trades`, as a plain integer.

Check your answer with:

    lab check 00_setup/01
"""

META: dict = {}


def solve(trades):
    raise NotImplementedError("Replace this line with your answer.")

"""Setup · your first DataFrame answer

Most exercises want a DataFrame back. The grader ignores your
index and your column order, but column *names* must match,
and by default row order does not matter — so you rarely need
a final sort.

TASK
    Return total notional (quantity * price) per sector, as a
    DataFrame with exactly two columns: `sector` and
    `notional`.

    Sector lives on `securities`, not on `trades`, so the two
    need joining.
"""

META: dict = {}


def solve(trades, securities):
    raise NotImplementedError("Replace this line with your answer.")
